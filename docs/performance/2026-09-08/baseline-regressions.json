{
  "baseCommit": "350823907b9f6b138fe760b9c8978dae129c7614",
  "purpose": "New regression tests run against unchanged main; failures are expected.",
  "numTotalTests": 22,
  "numPassedTests": 9,
  "numFailedTests": 13,
  "numPendingTests": 0,
  "success": false,
  "failedTests": [
    "cached serves every stale reader immediately while one refresh is pending",
    "cached does not resurrect invalidated data or release a newer request's slot",
    "cached retains stale data on background failure and retries only once for later readers",
    "getCacheStats uses each entry's configured TTL",
    "Cancello request volume and freshness reads a shared asset once and reuses unchanged bodies on the next snapshot",
    "Cancello request volume and freshness re-reads only edited bodies, while always re-reading current statuses and relations",
    "Cancello request volume and freshness an explicit refresh re-reads all bodies and concurrent refreshes share one crawl",
    "Cancello request volume and freshness an invalidated crawl cannot resurrect the old state or cache later queued bodies",
    "Cancello request volume and freshness keeps a linked calendar preview while reusing a page already returned by its query",
    "Questioni independent loading shows questions before the slower house summary resolves",
    "Questioni independent loading keeps questions usable when the summary fails and labels the error",
    "Questioni independent loading ignores an older response arriving after a refresh",
    "Questioni independent loading cancels both outstanding requests on navigation away"
  ]
}
